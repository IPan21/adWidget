import { renderAdItem } from './renderUtils.js';

export const renderOrganicItem = (ad) => {
    const branding = '';
    const targetType = '_self'
    return renderAdItem({ad, branding, targetType});
}